import Card from "../components/UI/Card";

const NotFound = () => {
    return (
        <Card>
            <p className="centered">Page not found!</p>
        </Card>
    );
};

export default NotFound;
